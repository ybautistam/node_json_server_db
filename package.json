{
  "name": "proyecto-json",
  "version": "0.0.1",
  "description": "armando un servidor de cursos ",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "json-server --watch db.json"

  },
  "author": "ybm",
  "license": "ISC",
  "dependencies": {
    "json-server": "^1.0.0-beta.0"
  }
}
